
@{
    Layout = null;
    @model List<Order_Food.Models.Chat>
}
<div id="re_chat">
    <table style="width:100%;margin:0 15px;">
        <tr>
            <td>
                user ซ้าย
            </td>
            <td>
                เเชท
            </td>
            <td>
                user ขวา
            </td>
        </tr>
        @foreach (var item in Model)
        {
            if (ViewBag.check == "u")
            {
                <tr>
                    @if (item.chexk == "u")
                    {
                        <td>
                            @item.customer_name
                        </td>
                        <td>
                            @item.chat1
                        </td>
                        <td></td>
                    }
                    else
                    {
                        <td></td>
                        <td>
                            @item.chat1
                        </td>
                        <td>
                            @item.user_name
                        </td>
                    }
                </tr>
            }
            else if (ViewBag.check == "c")
            {
                <tr>
                    @if (item.chexk == "c")
                    {
                        <td>
                            @item.user_name
                        </td>
                        <td>
                            @item.chat1
                        </td>
                        <td></td>
                    }
                    else
                    {
                        <td></td>
                        <td>
                            @item.chat1
                        </td>
                        <td>
                            @item.customer_name
                        </td>
                    }
                </tr>

            }
        }
    </table>
</div>
<script>
            setTimeout(function () {
                $("#re_chat").load("@Url.Action("Show_chat", "UsingU")");
            }, 3000);
</script>
